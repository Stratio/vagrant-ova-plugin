# Vagrant OVA Plugin
Vagrant plugin that exports a box from vbox to vmwware

##Install
$ vagrant plugin install vagrant-ova-0.1.0.gem

##Use
From the vagrant environment that you want to export to ova:

$ vagrant ova --output example.ova
